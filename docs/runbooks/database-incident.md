# Database incident

Declare incident, freeze nonessential writes, capture metrics/log correlations, validate replicas/backups and choose failover or restore. Never run destructive repair without an approved command, impact and rollback. After recovery run migrations/check constraints, reconcile outbox/audit and execute critical smoke tests.
