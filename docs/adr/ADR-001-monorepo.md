# ADR-001: pnpm monorepo

Accepted. Use `apps/web`, `apps/api`, `apps/worker` and shared packages for contracts, configuration, UI and testing. One lockfile makes contract drift and dependency review deterministic.
